#ifndef KLL_APP_H
#define KLL_APP_H

#include <intuition/intuition.h>

/* Runs the interface: title screen, main menu and the sections. */
void app_run(struct Screen *scr, struct Window *win);

#endif
